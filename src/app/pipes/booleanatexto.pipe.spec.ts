import { BooleanatextoPipe } from './booleanatexto.pipe';

describe('BooleanatextoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanatextoPipe();
    expect(pipe).toBeTruthy();
  });
});
